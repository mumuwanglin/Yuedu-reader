// Single source of truth for app + extension versioning.
// Wired as the project-level base configuration, so the main app, Widget,
// and ShareExtension all inherit these values. Bump here only.
MARKETING_VERSION = 1.0.5
CURRENT_PROJECT_VERSION = 1
